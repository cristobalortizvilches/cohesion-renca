---
title: "Estudio de Percepción y Evaluación Comunal de Renca 2023." 
subtitle: "Análisis descriptivo."
author: "Centro de Estudios de Conflicto y Cohesión Social"
date: '24-04-2023'
output:
  pdf_document:
    number_sections: true
  html_document:
    keep_md: yes
    theme: paper
    highlight: pygments
header-includes:
   - \usepackage{floatrow}
   - \floatsetup[figure]{capposition=top}
   - \usepackage{titling}
   - \pretitle{\begin{center}
     \includegraphics[width=4in,height=4in]{C:/Users/aigon/OneDrive/Documentos/GitHub/cohesion-renca/input/logos/COES_Logo_azul.jpg}\LARGE\\
     \includegraphics[width=4in,height=4in]{C:/Users/aigon/OneDrive/Documentos/GitHub/cohesion-renca/input/logos/renca.png}\LARGE\\}
   - \posttitle{\end{center}}
editor_options: 
  chunk_output_type: inline
---

\newpage

\tableofcontents

\newpage

```{r setup, include=FALSE}
knitr::opts_chunk$set(
	echo = FALSE,
	fig.align = "left",
	fig.topcaption = TRUE,
	fig.width = 10,
	fig.height = 8,
	message = FALSE,
	warning = FALSE,
	cache = FALSE
)
Sys.setlocale("LC_ALL","ES_ES.UTF-8")

```

```{r packages, message=FALSE, warning=FALSE, include=FALSE}
library(haven)
library(tidyverse)
library(psych)
library(gridExtra)
library(grid)
library(sjmisc)
library(summarytools)
library(sjlabelled)
library(ggplot2); theme_set(theme_bw() + 
    theme(axis.text=element_text(size=12),axis.title=element_text(size=13),
          strip.text.y = element_text(angle=0, size=13),plot.caption = element_text(size=11),
          panel.grid = element_line(color = "grey70",linetype = 2)))
```

```{r datos}
getwd()
renca <- read_dta("../input/data/Renca_1010_macrozona.dta")
load("../input/data/elsoc.sub.Rdata")

bar.plot <- function(x,weight){
  library(questionr)
  library(ggplot2)
  df <- as.data.frame(prop.table(wtd.table(x,weights=weight)))
  p <- ggplot(df,aes(x=Var1,y=Freq)) + geom_bar(stat="identity")  + theme_bw() + 
    theme(axis.text=element_text(size=12),axis.title=element_text(size=13),
          strip.text.y = element_text(angle=0, size=13),plot.caption = element_text(size=11),
          panel.grid = element_line(color = "grey70",linetype = 2))
  return(p)
}

elsoc.sub <- elsoc.sub %>% mutate_at(vars(starts_with("comuna")),function(x)gsub("nh","n",tolower(x))) %>%
  mutate(t05_w06 = factor(t05_w06,labels=c("Si, a otro barrio de la comuna","Si, a otra comuna","No, estoy bien donde estoy",
                                           "No, no puedo aunque quisiera")),
         t01_w06 = factor(case_when(
           t01_w06 %in% c(1,2) ~ 1,
           t01_w06 %in% 3 ~ 2,
           t01_w06 %in% c(4,5) ~ 3
         ),labels=c("Muy poco o Poco","Algo","Bastante o Mucho")),
         t08_w06 = factor(case_when(
           t08_w06 %in% c(1,2) ~ 1,
           t08_w06 %in% 3 ~ 2,
           t08_w06 %in% c(4,5) ~ 3
         ),labels=c("Negativamente","Ni positiva ni negativamente",
                                           "Positivamente")),
         t10_w06 = factor(case_when(
           t10_w06 %in% c(1,2) ~ 1,
           t10_w06 %in% 3 ~ 2,
           t10_w06 %in% c(4,5) ~ 3
         ),labels=c("Inseguro","Ni seguro ni inseguro",
                    "Seguro")),
         c07_01_w05 = factor(c07_01_w05,labels=c("No lo hizo","Lo hizo 1 o 2 veces",
                                                 "Lo hizo más de 2 veces"))) %>%
  mutate_at(vars(starts_with(c("t02","t03")) & ends_with("w06")),function(x)factor(case_when(
    x %in% c(1,2) ~ 1,
    x == 3 ~ 2,
    x %in% c(4,5) ~ 3
  ), labels = c("En desacuerdo","Ni de acuerdo \nni en desacuerdo","De acuerdo"))) %>% 
  mutate_at(vars(starts_with("c12"),-"c12_09_otro_w06"),function(x)factor(x,labels = c("No es miembro","Miembro \ninactivo",
                                                                    "Miembro \nactivo")))

#view(dfSummary(renca))
```

# Introducción.

## Presentación del Estudio

De acuerdo con la Ley N°18.625, Orgánica Constitucional de Municipalidades, son corporaciones autónomas de derecho público, con personalidad jurídica y patrimonio propio, que buscan satisfacer las necesidades de su comunidad y asegurar su participación en el progreso económico, social y cultural.

Para ello, deben velar por la correcta distribución de sus recursos, los que son destinados a programas y proyectos que buscan mejorar la calidad de vida de quienes viven en la comuna.

En virtud de lo anterior, la Ilustre Municipalidad de Renca encargó al Centro de Estudios de Conflicto y Cohesión Social (COES) la elaboración de un estudio que permita evaluar la percepción de los vecinos de la comuna respecto a distintos temas, identificar problemáticas y sus preferencias.

El presente documento presenta los principales resultados a nivel descriptivo, a partir de una encuesta realizada a 1.010 habitantes de la comuna.

## Ficha técnica

La siguiente tabla presenta el diseño muestral:

[AÑADIR CUADRO CON LA DESCRIPCION DE LA MUESTRA].

## Trabajo de campo

El levantamiento de los datos se realizó en el mes de marzo del año 2023, y fue realizado de manera presencial por un equipo de 19 encuestadores y encuestadoras.

## Muestra

La muestra total lograda son 1.010 casos. La siguiente tabla entrega el detalle por macrozona.

[AÑADIR TABLA]

\newpage

# Relación con la comuna y el barrio

## Apego al barrio

El Gráfico \ref{fig:apego_barrial} muestra a quienes se manifiestan como "Muy de acuerdo" o "De acuerdo" con las siguientes afirmaciones: (1) Este barrio es ideal para mi, (2) Me siento integrada/o en este barrio, (3) Me identifico con la gente de mi barrio, (4) Este barrio es parte de mi y (5) Me siento orgullosa/o de vivir en este barrio.

A partir de gráfico se logra ver que en la comuna las personas tienden a manifestar un cierto nivel de apego con su barrio: en torno al 50% de los encuestados manifiesta que se siente integrado en su barrio, que se identifica con la gente de su barrio o que el barrio es parte de ellos, aunque al mismo tiempo manifiestan que no se sienten orgullosos de vivir en el barrio: sólo un 40% manifiesta estar orgullos de vivir en su barrio, porcentaje relativamente similar a quienes manifiestan que el barrio donde viven es ideal para ellos.

```{r apego-barrial, echo=FALSE, fig.cap="\\label{fig:apego_barrial}Nivel de apego barrial"}
#remove("apego.barrio")

apego.barrio <- renca %>%
  dplyr::select(q0002_0001, q0002_0002, q0002_0003, q0002_0004, q0002_0005) 

apego.barrio <- apego.barrio %>% 
  pivot_longer(cols = names(apego.barrio))

apego.barrio <-  apego.barrio %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value %in% c(4,5)) %>% 
  summarise(sfreq = sum(freq))

apego.barrio <- apego.barrio %>% 
  mutate(name = factor(name, 
                       levels = c("q0002_0001", "q0002_0002", "q0002_0003", "q0002_0004", "q0002_0005"),
                       labels = c("Este barrio es\nideal para mi",
                                  "Me siento integrada/o\nen este barrio",
                                  "Me identifico con la\ngente de mi barrio",
                                  "Este barrio es\nparte de mi",
                                  "Me siento orgulloso/a\nde vivir en este barrio")))

apego.barrio %>%
  ggplot(aes(x = name, y = sfreq, 
             label = as.character(scales::percent(sfreq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Nivel de apego al barrio", subtitle = "Porcentaje que responde 'de acuerdo o muy de acuerdo'", 
       x = "", y = "") 
```

El Gráfico \ref{fig:apego_barrial_elsoc} muestra algunos de los ítems del Gráfico anterior pero comparados con la Región Metropolitana, ocupando los datos de la Encuesta Longitudinal Social de Chile (ELSOC) en su sexta ola.

Lo que se observa es que estos porcentajes son en algunos casos similares a lo que muestra la Región Metropolitana en general, aunque siempre por debajo entre unos 5 y 10 puntos.

\newpage

```{r apego-barrial-elsoc, echo=FALSE, fig.cap="\\label{fig:apego_barrial_elsoc}Nivel de apego barrial (ELSOC)"}
vars <- c("Este barrio es ideal para mi","Me siento integrado en este barrio", #t02
          "Me identifico con la gente de este barrio","Este barrio es parte de mi",
          "En este barrio es fácil hacer amigos","La genete de este barrio es sociable", #t03
          "La gente de este barrio es cordial","La gente de este barrio es colaboradora")

elsoc_barrio <- elsoc.sub %>% select(starts_with("t02"))
elsoc_barrio <- elsoc_barrio %>% pivot_longer(cols=names(elsoc_barrio))
elsoc_barrio <- elsoc_barrio %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% na.omit() %>%
  mutate(name = factor(name,
                       levels = c("t02_01_w06","t02_02_w06","t02_03_w06","t02_04_w06"),
                       labels = c("Este barrio es ideal para mi","Me siento integrado en este barrio",
          "Me identifico con la gente de este barrio","Este barrio es parte de mi"))) %>% filter(value == "De acuerdo")

elsoc_barrio %>%
  ggplot(aes(x = name, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Nivel de apego al barrio", subtitle = "Porcentaje que responde 'de acuerdo o muy de acuerdo'", 
       x = "", y = "", caption = "Fuente: elaboración propia a partir de ELSOC Ola 6.") +
  coord_flip()

```

\newpage

## Sociabilidad

El Gráfico \ref{fig:soci_barrial} muestra el grado de acuerdo que existe con las siguientes frases: (1) La gente en este barrio está dispuesta a ayudar a otros residentes cuando es necesario, (2) La gente en este barrio está interesada en los demás, (3) La gente en este barrio es colaboradora, (4) La gente en este barrio es cordial, (5) La gente en este barrio es sociable y (6) En este barrio es fácil hacer amigos.

Al igual que en el gráfico anterior, se observa en general una buena valoración de la vida en el barrio. En general, las personas tienden a evaluar de manera positiva a sus vecinos; solamente el interés por los demás cae por debajo del 50%.

```{r soci-barrial, echo=FALSE, fig.cap="\\label{fig:soci_barrial}Percepción de sociabilidad barrial"}
#remove("soci.barrial")

soci.barrial <- renca %>%
  dplyr::select(q0002_0007, q0002_0008, q0002_0009, q0002_0010, q0002_0011, q0002_0012) 

soci.barrial <- soci.barrial %>% 
  pivot_longer(cols = names(soci.barrial))

soci.barrial <-  soci.barrial %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value %in% c(4,5)) %>% 
  summarise(sfreq = sum(freq))

soci.barrial <- soci.barrial %>% 
  mutate(name = factor(name, 
                       levels = c("q0002_0007", "q0002_0008", "q0002_0009", "q0002_0010", "q0002_0011", "q0002_0012"),
                       labels = c("En este barrio es\nfácil hacer amigas/os",
                                  "La gente en este\nbarrio es sociable",
                                  "La gente en este\nbarrio es cordial",
                                  "La gente en este\nbarrio es colaboradora",
                                  "La gente en este\nbarrio está interesada en los demás",
                                  "La gente en este\nbarrio está dispuesta a ayudar a\notros residentes cuando es necesario")))

soci.barrial %>%
  ggplot(aes(x = name, y = sfreq, 
             label = as.character(scales::percent(sfreq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Nivel de sociabilidad barrial", subtitle = "Porcentaje que responde 'de acuerdo o muy de acuerdo'", 
       x = "", y = "") +
  coord_flip()
```

Nuevamente comparando con la Región Metropolitana se observa que los patrones difieren en algunos aspectos. Por ejemplo, si en Renca el 53% considera que en el barrio es fácil hacer amigos, en la RM el 44% piensa lo mismo. Por el contrario, si en Renca el 54% considera que la gente de su barrio es cordial, este porcentaje aumenta a un 65% para la RM. En lo que respecta a la percepción de colaboración y a la de sociabilidad, no se observan grandes diferencias.

```{r soci-barrial2, echo=FALSE, fig.cap="\\label{fig:soci_barrial_elsoc}Percepción de sociabilidad barrial (ELSOC)"}

elsoc_barrio2 <- elsoc.sub %>% select(starts_with("t03"))
elsoc_barrio2 <- elsoc_barrio2 %>% pivot_longer(cols=names(elsoc_barrio2))
elsoc_barrio2 <- elsoc_barrio2 %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% na.omit() %>%
  mutate(name = factor(name,
                       levels = c("t03_01_w06","t03_02_w06","t03_03_w06","t03_04_w06"),
                       labels = c("En este barrio es fácil hacer amigos",
                                  "La gente de este barrio es sociable","La gente de este barrio es cordial","La gente de este barrio es colaboradora"))) %>% filter(value == "De acuerdo")

elsoc_barrio2 %>%
  ggplot(aes(x = name, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Nivel de sociabilidad barrial", subtitle = "Porcentaje que responde 'de acuerdo o muy de acuerdo'", 
       x = "", y = "", caption = "Fuente: elaboración propia a partir de ELSOC Ola 6.") +
  coord_flip()

```

```{r amigos-barrio,fig.cap="\\label{fig:n_amigos}Número de amigos en el barrio"}
amigos.barrio <- renca %>%
  dplyr::select(q0003) %>% 
  as_label(q0003) %>% 
  count(q0003) %>% 
  mutate(freq = n/sum(n))

amigos.barrio %>%
  ggplot(aes(x = q0003, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Cuántos amigos o amigas cercanas tiene usted en el barrio?", x = "", y = "") 
```

\newpage

### Apoyo social

El Gráfico \ref{fig:apoyo_social} muestra el nivel de apoyo social que las personas perciben que pueden recibir de sus vecinos. En concreto, el gráfico muestra el porcentaje de personas que considera tener al menos un vecino a quien pedirle ayuda según distintas situaciones.

La mayoría de los consultados reporta que puede solicitarle a un vecino ayuda con temas personales (55% aproximadamente). Sin embargo, este porcentaje desciende para las otras actividades, llegando al 38% que considera que puede pedir ayuda en caso de enfermedad de algún miembro del hogar. Esto puede ser indicativo de que las posibilidades de recibir ayuda disminuyen conforme la dificultad del problema aumenta.

```{r apoyo-social, fig.cap="\\label{fig:apoyo_social}Nivel de apoyo social"}
#remove("apoyo.social")

apoyo.social <- renca %>%
  dplyr::select(starts_with("q0004_")) 

apoyo.social <- apoyo.social %>% 
  pivot_longer(cols = names(apoyo.social))


apoyo.social <-  apoyo.social %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value == 1) 

apoyo.social <- apoyo.social %>% 
  mutate(name = factor(name,
                       levels = c("q0004_0001", "q0004_0002", "q0004_0003", "q0004_0004", "q0004_0005", "q0004_0006"),
                       labels = c("Ayudar en el cuidado en caso\nde enfermedad de\nalgún miembro del hogar",
                                  "Ayudar en el cuidado de niños(as)\nu otras personas de su hogar",
                                  "Prestar dinero en\ncaso de emergencia",
                                  "Ayudar a resolver consultas\no realizar trámites legales o financieros",
                                  "Ayudar con reparaciones\ndel hogar",
                                  "Apoyar en caso de\nproblemas personales"))) 
apoyo.social %>%
  ggplot(aes(x = name, y = freq, label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Tiene algun vecino o vecina que pueda ayudarle\no apoyarle en los siguientes asuntos?", 
       subtitle = "Porcentaje que responde 'Sí'", 
       x = "", y = "") +
  coord_flip()
```

\newpage

### Visitas

El Gráfico \ref{fig:visitas} muestra el número de visitas que los encuestados han realizado en el último mes a sus vecinos.

Según el gráfico, cerca de un 75% ha visitado alguna vez a sus vecinos: un 37% los ha visitado una o dos veces en el último mes, mientras que el 38% lo ha hecho más de dos veces. Poco más de un 20% declara no haber visitado a sus vecinos en el último mes.

```{r cant-visit,fig.cap="\\label{fig:visitas}Número de visitas a un vecinó ralizadas en el último mes"}
visitas <- renca %>%
  dplyr::select(q0005) %>% 
  as_label(q0005) %>% 
  count(q0005) %>% 
  mutate(freq = n/sum(n)) %>% filter(q0005 != "No sabe (no leer)", q0005 != "No responde (no leer)")

visitas %>%
  ggplot(aes(x = q0005, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = get_label(renca$q0005), x = "", y = "") 
```

Ahora, comparado con la RM, se observa que, de acuerdo al Gráfico \ref{fig:visitas.elsoc}, en Renca hay una mayor frecuencia de visitas a los vecinos: si en la RM el 33% declara nunca haber visitado a un vecino, en Renca este porcentaje baja al 22%. A su vez, si en la RM entre el 15% y el 26% declara haber visitado 1 o 2 veces a su vecino, o más de 2 veces, estos porcentajes alcanzan el 37% y 38% en Renca.

```{r cant-visit.elsoc,fig.cap="\\label{fig:visitas.elsoc}Número de visitas a un vecinó ralizadas en el último mes (ELSO)"}
visitas.elsoc <- elsoc.sub %>%
  dplyr::select(c07_01_w05) %>% 
  as_label(c07_01_w05) %>% 
  count(c07_01_w05) %>% 
  mutate(freq = n/sum(n)) %>% na.omit()

visitas.elsoc %>%
  ggplot(aes(x = c07_01_w05, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = get_label(renca$q0005), x = "", y = "",
       caption = "Fuente: elaboración propia a partir de la ELSOC Ola 5") 
```

\newpage

### Confianza

El Gráfico \ref{fig:conf_vec} muestra el nivel de confianza que se tiene en los vecinos. De acuerdo con los datos. En términos generales, la distribución de los niveles de confianza es bastante similar, aunque tiende a concentrarse en categorías medias de confianza. Cerca de un 35% no confía en sus vecinos (un 10% declara no confiar en nada, mientras que un 25% declara confiar poco). Un porcentaje similar se observa entre quienes declaran confiar mucho o bastante en sus vecinos: mientras un 3.6% declara confiar mucho en sus vecinos, un 28.4% declara que confía bastante. Finalmente, un 32% confía algo en sus vecinos.

```{r conf-vecinal,fig.cap="\\label{fig:conf_vec}Nivel de confianza en los vecinos"}
confianza <- renca %>%
  dplyr::select(q0006) %>% 
  filter(!q0006 == 6) %>% 
  as_label(q0006) %>% 
  count(q0006) %>% 
  mutate(freq = n/sum(n)) %>% filter(q0006 != "NR (No leer)")
  

confianza %>%
  ggplot(aes(x = q0006, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "En términos generales, ¿Cuánto confía usted en sus vecinos?", x = "", y = "") 
```

El Gráfico \ref{fig:conf_vec_elsoc} muestra los niveles de confianza entre los vecinos para la RM. En ella se ve que el 42% de los habitantes de la RM confía en sus vecinos, situación que contrasta con la de Renca. Sin embargo, si se observa a quienes no confían se ve que las diferencias son menos marcadas con respecto a la RM, en donde el 29% declara no confiar en sus vecinos. Es decir, en comparación a la RM, si bien en Renca son menos los que confían mucho o bastante en sus vecinos, sí son más los que confían en algo en sus vecinos.

```{r conf-vecinal.elsoc,fig.cap="\\label{fig:conf_vec_elsoc}Nivel de confianza en los vecinos (ELSOC)"}
confianza.elsoc <- elsoc.sub %>%
  dplyr::select(t01_w06) %>% 
  as_label(t01_w06) %>% 
  count(t01_w06) %>% 
  mutate(freq = n/sum(n)) %>% na.omit()
  

confianza.elsoc %>%
  ggplot(aes(x = t01_w06, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "En términos generales, ¿Cuánto confía usted en sus vecinos?", x = "", y = "",
       caption= "Fuente: elaboración propia a partir de la ELSOC Ola 6.") 
```

\newpage

## Confianza en autoridades e instituciones comunales

Por su parte, el Gráfico \ref{fig:conf_auto} muestra el porcentaje de personas que manifiesta tener bastante o mucha confianza en las autoridades y servicios comunales. Se observa que los establecimientos educacionales y los servicios de salud muestran los mayores niveles de confianza (en torno al 40% manifiesta confiar mucho o bastante en ellos), lo que contrasta con la categoría de servicios municipales: poco más de un 20% manifiesta confiar mucho o bastante en ellos.

Por su parte, las autoridades políticas (concejales y alcalde), tienen los menores niveles de confianza: un 12.5% para los concejales manifiesta confiar mucho o bastante en los concejales, y un 17% manifiesta lo mismo para el alcalde; porcentajes similares a los de las juntas de vecinos: un 14% manifiesta tener mucha o bastante confianza en ellas.

```{r conf-auto,fig.cap="\\label{fig:conf_auto}Nivel de confianza en autoridades y servicios comunales"}
#remove("conf.auto")

conf.auto <- renca %>%
  dplyr::select(starts_with("q0007_")) 

conf.auto <- conf.auto %>% 
  pivot_longer(cols = names(conf.auto)) 

conf.auto <-  conf.auto %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value %in% c(4,5)) %>% 
  summarise(sfreq = sum(freq)) %>% 
  mutate(name = factor(name, 
                       levels = c("q0007_0001", "q0007_0002", "q0007_0003", 
                                  "q0007_0004", "q0007_0005", "q0007_0006",
                                  "q0007_0007"),
                       labels = c("El alcalde de Renca",
                                  "Concejales de Renca",
                                  "Servicios municipales",
                                  "Centros de salud",
                                  "Establecimientos\neducacionales",
                                  "Juntas de vecinos",
                                  "Carabineros que\ntrabajan en la comuna")))

conf.auto %>%
  ggplot(aes(x = name, y = sfreq, 
             label = as.character(scales::percent(sfreq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Grado de confianza en instituciones o autoridades de la comuna", 
       subtitle = "Porcentaje que responde 'bastante o mucho'", 
       x = "", y = "") +
  coord_flip()
```

\newpage

## Arraigo físico

El Gráfico \ref{fig:arraigo} muestra el porcentaje de personas que se cambiaría de domicilio en el próximo año. A partir de él se logra ver que la gran mayoría, es decir, más del 80% de los encuestados manifiesta que no se cambiaría de su actual domicilio, aunque las razones difieren: mientras el 50% manifiesta que no lo haría porque considera que está bien en su actual domicilio, el 32% no lo haría porque no tiene los medios económicos suficientes para ello.

```{r arraigo,fig.cap="\\label{fig:arraigo}Probabilidad de cambio de domicilio"}
arraigo <- renca %>%
  dplyr::select(q0008) %>% 
  filter(!q0008 %in% c(5,6)) %>% 
  as_label(q0008) %>% 
  count(q0008) %>% 
  mutate(freq = n/sum(n))
  

arraigo %>%
  ggplot(aes(x = q0008, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Tiene planeado cambiarse de casa/departamento en el próximo año?", x = "", y = "") 
```

De acuerdo con el Gráfico \ref{fig:arraigo.elsoc} no parecen haber grandes diferencias entre la RM y Renca en este sentido.

```{r arraigo.elsoc,fig.cap="\\label{fig:arraigo.elsoc}Probabilidad de cambio de domicilio (ELSOC)"}
arraigo.elsoc <- elsoc.sub %>%
  dplyr::select(t05_w06) %>%
  as_label(t05_w06) %>% 
  count(t05_w06) %>% 
  mutate(freq = n/sum(n)) %>% na.omit()
  

arraigo.elsoc %>%
  ggplot(aes(x = t05_w06, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Tiene planeado cambiarse de casa/departamento en el próximo año?", x = "", y = "",
       caption = "Fuente: elaboración propia a partir de la ELSOC Ola 6.") 
```

\newpage

## Participación

El Gráfico \ref{fig:participacion} muestra el porcentaje de personas que participa en distintos tipos de organizaciones. Un tercio de los encuestados declara participar en organizaciones religiosas o ligadas a la iglesia, proporción similar a quienes declaran participar en clubes deportivos.

Después, cerca de un 20% de los encuestados declara participar en comités de vivienda, de adelanto y/o de administración o en agrupaciones de adulto mayor, y un cuarto en juntas de vecinos u organizaciones territoriales. Un 15% declara participar en centros de madres, talleres de mujeres u organizaciones de mujeres. El resto de las categorías cae por debajo del 10%.

```{r participacion2,fig.cap="\\label{fig:participacion}Porcentaje de personas que participa en alguna asociación"}
#remove("particip")

particip <- renca %>%
  dplyr::select(starts_with("q0009_"), -q0009_0017, -q0009_0018, -q0009_0019, -q0009_other) %>% 
  as_label()

particip <- particip %>% 
  pivot_longer(cols = names(particip)) 

particip <-  particip %>% 
  count(name, value) %>% 
  drop_na() %>% 
  mutate(freq = (n/sum(n)))

particip %>%
  ggplot(aes(x = value, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Participación en organizaciones", 
       subtitle = "En los últimos 12 meses", 
       x = "", y = "") +
  coord_flip()
```

Si se comparan estos resultados con la RM en su conjunto, se observa que, de acuerdo al Gráfico \ref{fig:prticipacion.elsco}, hay similitudes con la región en lo que refiere a la tendencia, es decir: las organizaciones religiosas son las que cuentan con la mayor cantidad de participantes. Sin embargo, existen diferencias significativas en lo que respecta a las magnitudes. Si en Renca el 9% participa en organizaciones religiosas, en la RM este porcentaje alcanza el 26%. A su vez, si en Renca el 7% participa en las juntas de vecinos u organizaciones territoriales, este porcentaje alcanza un 27% en la RM

```{r participacion.elsoc,fig.cap="\\label{fig:prticipacion.elsoc}Porcentaje de personas que participa en alguna asociación (ELSOC)"}
#remove("particip")

particip.elsoc <- elsoc.sub %>%
  select(starts_with("c12"), -starts_with(c("c12_09","c12_06"))) %>% mutate_all(function(x)ifelse(x %in% c("Miembro \ninactivo","Miembro \nactivo"),"Sí",x))

particip.elsoc <- particip.elsoc %>% 
  pivot_longer(cols = names(particip.elsoc)) 

particip.elsoc <-  particip.elsoc %>% 
  count(name, value) %>% 
  drop_na() %>% 
  group_by(name) %>%
  mutate(freq = (n/sum(n)),
         name = factor(name,
                       levels = c("c12_01_w06","c12_02_w06","c12_03_w06","c12_04_w06",
                                  "c12_05_w06","c12_07_w06","c12_08_w06"),
                       labels = c("Junta de vecinos","Org. religiosa","Partido o movimiento político",
          "Sindicato","Asociación \nprofesional o gremial","Org. deportiva","Asociaciones o \ncentro de estudiantes"))) %>% filter(value=="Sí")

particip.elsoc %>%
  ggplot(aes(x = name, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Participación en organizaciones", 
       subtitle = "Miembros activos e inactivos", 
       x = "", y = "", caption = "Fuente: elaboración propia a partir de ELSOC Ola 6") +
  coord_flip()
```

\newpage

## Actividad civica

El Gráfico \ref{fig:act_civica} muestra el porcentaje de personas que declara haber participado en distintas instancias de activismo. En general se observa que, entre quienes participan, la mayoría ha ocupado las redes sociales para expresar sus opiniones (35%) seguido de la asistencia a reuniones (29%).

```{r actividad-civica,fig.cap="\\label{fig:act_civica}Participación en actividades civicas"}
#remove("actividad.civica")
actividad.civica <- renca %>%
  dplyr::select(starts_with("q0011_")) 

actividad.civica <- actividad.civica %>% 
  pivot_longer(cols = names(actividad.civica))

actividad.civica <-  actividad.civica %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value == 1) 

actividad.civica <- actividad.civica %>% 
  mutate(name = factor(name,
                       levels = c("q0011_0001", "q0011_0002", "q0011_0003", "q0011_0004"),
                       labels = c("Asistido a alguna reunión donde se traten temas\nde interés público o comunitario dentro de la comuna",
                                  "Firmado una carta o petición apoyando una\ncausa relacionada con la comuna",
                                  "Asistido a una marcha o manifestación\npolítica dentro de la comuna",
                                  "Usado las redes sociales para expresar su\nopinión en temas públicos sobre la comuna"))) 
actividad.civica %>%
  ggplot(aes(x = name, y = freq, label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Durante los últimos 12 meses,\n¿con cuánta frecuencia usted ha...?", 
       subtitle = "Porcentaje que responde 'Sí'", 
       x = "", y = "") +
  coord_flip()

```

\newpage

## Grupos de nuevos residentes

El Gráfico \ref{fig:nvo_residente} muestra el grado de aceptación a la llegada al barrio de distintos grupos sociales, tales como inmigrantes, personas de estratos socio-económicos medios, etc.

De acuerdo con los datos, los grupos cuya llegada tendría menor aceptación son los inmigrantes, principalmente venezolanos y colombianos (64% no estaría de acuerdo con su llegada al barrio) y haitianos (61% no estaría de acuerdo con su llegada al barrio), seguidos de las personas que hayan estado privadas de libertad (61% no estaría de acuerdo con su llegada al barrio). Más abajo, estarían los inmigrantes peruanos y bolivianos: en este caso poco menos del 50% no estaría de acuerdo con su llegada al barrio.

Estos resultados contrastan con los otros grupos sociales, en donde su rechazo tiende a no superar el 20% aproximadamente, como es el caso de las personas de la diversidad sexual, de pueblos originarios.

```{r nuevos-residentes,fig.cap="\\label{fig:nvo_residente}Aceptación de nuevos residentes según origen"}
#remove("new.resid")
new.resid <- renca %>%
  dplyr::select(starts_with("q0012_")) 

new.resid <- new.resid %>% 
  pivot_longer(cols = names(new.resid)) 

new.resid <-  new.resid %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value %in% c(1,2)) %>% 
  summarise(sfreq = sum(freq))

new.resid <- new.resid %>% 
  mutate(name = factor(name, 
                       levels = c("q0012_0001", "q0012_0002", "q0012_0003", "q0012_0004", "q0012_0005",
                                  "q0012_0006", "q0012_0007", "q0012_0008", "q0012_0009"),
                       labels = c("Personas  de  pueblos  indígenas",
                                  "Personas de clase media acomodada",
                                  "Personas que hayan estado\nprivadas de libertad (ex presos)",
                                  "Personas de clase baja",
                                  "Inmigrantes peruanos y bolivianos",
                                  "Inmigrantes venezolanos y colombianos",
                                  "Inmigrantes haitianos",
                                  "Diversidades  sexuales \n(Homosexuales, lesbianas, transexuales, etc.)",
                                  "Inmigrantes norteamericanos o europeos")))

new.resid %>%
  ggplot(aes(x = name, y = sfreq, 
             label = as.character(scales::percent(sfreq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Grado de acuerdo con la llegada de:", subtitle = "Porcentaje que responde 'en desacuerdo o muy en desacuerdo'", 
       x = "", y = "") +
  coord_flip()
```

\newpage

## Justificación violencia

El Gráfico \ref{fig:just_viol} muestra el porcentaje de personas que justificarían el uso de la violencia para detener a un delincuente, o bien como medio de protesta.

En términos generales, se observa un bajo apoyo a la violencia como medio de protesta: menos de un 5% estaría de acuerdo con dañar el mobiliario o el transporte público para exigir cambios sociales. Situación que contrasta con el apoyo a la violencia para detener a un delincuente. En este caso, cerca de un 8% estaría de acuerdo con hacer una detención ciudadana, esto es: amarrarlo a un poste y desnudarlo. Por su parte, un 15% estaría a favor de perseguir y golpear a quien acaba de cometer un asalto.

```{r just-viol,fig.cap="\\label{fig:just_viol}Porcentaje de personas que justifican la violencia según casos"}
#remove("just.viol")
just.viol <- renca %>%
  dplyr::select(starts_with("q0013_")) 

just.viol <- just.viol %>% 
  pivot_longer(cols = names(just.viol)) 

just.viol <-  just.viol %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value %in% c(4,5)) %>% 
  summarise(sfreq = sum(freq))

just.viol <- just.viol %>% 
  mutate(name = factor(name, 
                       levels = c("q0013_0001", "q0013_0002", "q0013_0004", "q0013_0005"),
                       labels = c("Que algunas personas incendien o dañen\nmobiliario público, negocios, y/o\nlocales para exigir cambios sociales",
                                  "Que algunas personas incendien o dañen\nmedios de transporte para exigir\ncambios sociales",
                                  "Que algunas personas persigan y golpeen\na un “delincuente” que acaba de\ncometer un asalto",
                                  "Que algunas personas amarren a un poste\ny desnuden a un “delincuente” que\nacaba de cometer un asalto")))

just.viol %>%
  ggplot(aes(x = name, y = sfreq, 
             label = as.character(scales::percent(sfreq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿En qué medida se justifican las siguientes\nsituaciones dentro de la comuna?", 
       subtitle = "Porcentaje que responde 'muchas veces o siempre se justifica'", 
       x = "", y = "") +
  coord_flip()
```

\newpage

# Conocimiento de iniciativas municipales y autoridades

## Medios de información:

El Gráfico \ref{fig:medio_info} muestra cuáles son los principales medios por los cuales los vecinos de Renca se informan. De acuerdo con la información del gráfico, la mayoría utiliza medios digitales para informarse, destacando Facebook (con un 21%) y medios online (13%), sin embargo, sólo un 7% se informa por medio de Instagram, y apenas un 1% por medio de Twitter, y un 0.5% por medio de TikTok. Por su parte, un 13% declara que se informa por medio de los vecinos.

Por otro lado, cuando se observan los medios de comunicación tradicionales, se observa que el 13% prefiere la televisión, y apenas un 1.5% la radio y el diario de la comuna.

```{r medio-inform,fig.cap="\\label{fig:medio_info}Principales medios de información usados por los vecinos"}
renca %>%
  filter(!q0014 %in% 16:17) %>% 
  as_label(q0014) %>% 
  count(q0014) %>%
  mutate(freq = (n/sum(n))) %>%
  drop_na() %>% 
  ggplot(aes(x = q0014, y = freq, label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Cuál es el principal medio por donde informarse?", x = "", y = "") +
  coord_flip()
```

Por otro lado, de acuerdo al Gráfico \ref{fig:conocimiento_medios} se evidencia un bajo nivel de conocimiento de distintos medios de información, a saber: el WhatsApp "Renca Responde", la plataforma "Renca Participa" y el Diario "El Mirador de Renca". En los dos primeros, el grado de conocimiento no supera el 20%, mientras que en el caso del diario de la comuna, este porcentaje sube al 30% aproximadamente. Estos resultados bien pueden explicar el por qué los vecinos de la comuna no los mencionan como los principales medios por los cuales se informan.

```{r conoce-medios,fig.cap="\\label{fig:conocimiento_medios}Grado de conocimiento de los medios de comunicación comunales"}
wsp <- renca %>% 
  filter(!q0015 %in% 3:4) %>% 
  as_label(q0015) %>% 
  count(q0015) %>%
  mutate(freq = (n/sum(n))) %>%
  drop_na() %>% 
  ggplot(aes(x = q0015, y = freq, label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -1, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Whatsapp informativo “RencaResponde”", x = "", y = "")

app <- renca %>% 
  filter(!q0016 %in% 3:4) %>% 
  as_label(q0016) %>% 
  count(q0016) %>%
  mutate(freq = (n/sum(n))) %>%
  drop_na() %>% 
  ggplot(aes(x = q0016, y = freq, label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -1, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Pataforma “Renca Participa”", x = "", y = "")

diario <- renca %>% 
  filter(!q0017 %in% 3:4) %>%  
  as_label(q0017) %>% 
  count(q0017) %>%
  mutate(freq = (n/sum(n))) %>%
  drop_na() %>% 
  ggplot(aes(x = q0017, y = freq, label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -1, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Diario “El Mirador deRenca”", x = "", y = "")

grid.arrange(wsp,app,diario ,nrow = 2, top = textGrob("Usted conoce:"))
```

## Evaluación infraestructura y programas municipales.

El Gráfico \ref{fig:conoce_infra} muestra el grado de conocimiento de distintos lugares de la comuna. A diferencia de los medios de comunicación, se ve que existe un alto conocimiento en general de los distintos espacios de la comuna, destacando la Plaza Mayor con un 84% de conocimiento, seguida del Estadio Municipal con un 82%. Por su parte, el Parque Cerros de Renca muestra un 73% de conocimiento.

En contraste, existen tres lugares que bajan del 50% de conocimiento. "La Fabrica" es el lugar con el menor nivel de conocimiento: sólo un 28% la conoce. Por otro lado, los estadios Sarmiento y Newén también bajan del 50% de conocimiento, alcanzando un 40% de conocimiento aproximadamente.

```{r conoce-infra,fig.cap="\\label{fig:conoce_infra}Grado de conocimiento de infraestructura comunal"}
#remove("conoce.infra")

conoce.infra <- renca %>%
  dplyr::select(starts_with("q0018_"), -ends_with("_0002")) 

conoce.infra <- conoce.infra %>% 
  pivot_longer(cols = names(conoce.infra))


conoce.infra <-  conoce.infra %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value == 1) 

conoce.infra <- conoce.infra %>% 
  mutate(name = factor(name,
                       levels = c("q0018_0001_0001", "q0018_0002_0001", "q0018_0003_0001",
                                  "q0018_0004_0001", "q0018_0005_0001", "q0018_0006_0001",
                                  "q0018_0007_0001", "q0018_0008_0001", "q0018_0009_0001",
                                  "q0018_0010_0001", "q0018_0011_0001"),
                       labels = c("Punto Limpio del Parque las Palmeras", 
                                  "“La Fábrica” de Renca", 
                                  "Estadio Municipal de Renca", "Estadio Newén", 
                                  "Estadio Sarmiento", "Gimnasio Bicentenario", 
                                  "Parque Las Palmeras",
                                  "Edificio Municipal", "El Parque Cerros de Renca", 
                                  "Casa de la Cultura", "Plaza Mayor"))) 
conoce.infra %>%
  ggplot(aes(x = name, y = freq, label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Conoce o ha escuchado hablar de:", subtitle = "Porcentaje que responde 'Sí'", 
       x = "", y = "") +
  coord_flip()
```

\newpage

Por su parte, el Gráfico \ref{fig:notas_infra} muestra la evaluación que los vecinos hacen de las instalaciones de la comuna. En promedio, cada una de las instalaciones recibe una nota de 4.5, siendo el Estadio Municipal y el Gimnasio Bicentenario los que mejor evaluación obtienen con un 5 aproximadamente. Por su parte, cuando se observa a aquellas instalaciones con menor nivel de conocimiento, de acuerdo al gráfico anterior, se constata que "La Fabrica" es la que recibe la peor nota con un 3.9 aproximadamente, en cambio los Estadios Sarmiento y Newén reciben una nota apenas por sobre el 4.

```{r nota-infra,fig.cap="\\label{fig:notas_infra}Nota a la infraestructura comunal"}
#remove("nota.infra")

nota.infra <- renca %>%
  dplyr::select(starts_with("q0018_"), -ends_with("_0001")) 

nota.infra <- nota.infra %>% 
  pivot_longer(cols = names(nota.infra))

nota.infra <-  nota.infra %>% 
  filter(!value %in% c(8,9,10)) %>% 
  group_by(name) %>% 
  summarise(mean = mean(value))

nota.infra <- nota.infra %>% 
  mutate(name = factor(name,
                       levels = c("q0018_0001_0002", "q0018_0002_0002", "q0018_0003_0002",
                                  "q0018_0004_0002", "q0018_0005_0002", "q0018_0006_0002",
                                  "q0018_0007_0002", "q0018_0008_0002", "q0018_0009_0002",
                                  "q0018_0010_0002", "q0018_0011_0002"),
                       labels = c("Punto Limpio del Parque las Palmeras", 
                                  "“La Fábrica” de Renca", 
                                  "Estadio Municipal de Renca", "Estadio Newén", 
                                  "Estadio Sarmiento", "Gimnasio Bicentenario", 
                                  "Parque Las Palmeras",
                                  "Edificio Municipal", "El Parque Cerros de Renca", 
                                  "Casa de la Cultura", "Plaza Mayor")))

ggplot(nota.infra, aes(x = name, y = mean)) +
  geom_point(size = 3) + 
  geom_segment(aes(x = name, xend = name, y = 0, yend = mean)) + 
  scale_y_continuous(limits = c(0, 7)) +
  labs(title = "¿Cómo evaluaría la calidad en una escala de 1 a 7?",
       subtitle = "Evaluación promedio de la muestra",
       x = "", y = "",
       caption = paste("Promedio = ",round(mean(nota.infra$mean),1))) +
  coord_flip()
```

\newpage

En cuanto al Gráfico \ref{fig:conoce_prog} muestra el grado de conocimiento de distintos programas de la comuna. En este caso, existe una dispar nivel de conocimiento entre programas, aunque varios de ellos tienden a tener niveles de conocimiento bajos.

Entre los que mayor nivel de conocimiento tienen, destacan tres: la Farmacia Comunitaria, con un 82% de conocimiento. En un segundo lugar se encuentra el Servicio de Seguridad Comunitario con un 75% de conocimiento, y finalmente, con un 63% de conocimiento se encuentra la Óptica Comunitaria.

Por su parte, los tres programas con menores niveles de conocimiento vendrían siendo el Programa Fuerza Joven con un 21% de conocimiento, el programa Renca Jazz con un 22% y la Escuela del trabajo y el PADI, ambos con un 25% de conocimiento aproximadamente.

```{r conoce-programas,fig.cap="\\label{fig:conoce_prog}Grado de conocimiento de programas municiaples"}
#remove("conoceprogramas")

conoce.programas <- renca %>%
  dplyr::select(starts_with("q0019_"), -ends_with("_0002")) 

conoce.programas <- conoce.programas %>% 
  pivot_longer(cols = names(conoce.programas))

conoce.programas <-  conoce.programas %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value == 1) 

conoce.programas <- conoce.programas %>% 
  mutate(name = factor(name,
                       levels = c("q0019_0001_0001", "q0019_0002_0001", "q0019_0003_0001",
                                  "q0019_0004_0001", "q0019_0005_0001", "q0019_0006_0001",
                                  "q0019_0007_0001", "q0019_0008_0001", "q0019_0009_0001",
                                  "q0019_0010_0001", "q0019_0011_0001", "q0019_0012_0001",
                                  "q0019_0013_0001", "q0019_0014_0001"),
                       labels = c("Farmacia Comunitaria ", "Óptica Comunitaria", 
                       "Servicios de acompañamiento (PADI)", "Renca Jazz", 
                       "Santiago a Mil en Renca", "Programa Fuerza Joven", "Programa 75+", 
                       "Agenda Fácil", "Servicio de seguridad comunitaria 1453",  
                       "Estrategia de prevención de \nviolencia contra la mujer", 
                       "Atención municipal vespertina", "Renca te abriga", 
                       "Escuela del Trabajo" ,"Corrida Familiar")))

conoce.programas %>%
  ggplot(aes(x = name, y = freq, label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Conoce o ha escuchado hablar de:", subtitle = "Porcentaje que responde 'Sí'", 
       x = "", y = "") +
  coord_flip()
```

\newpage

Ahora, cuando se evalúan los distintos programas se observa que, de acuerdo con el Gráfico \ref{fig:nota_programas} se observa que, en promedio, los programas son evaluados con una nota de 4.5, siendo la Farmacia Comunitaria la con mejor evaluación: un 5 aproximadamente, nota similar a la que recibe el programa Santiago a Mil en Renca. La óptica comunitaria obtiene un 4.8 aproximadamente. Sin embargo, cuando se observa el Servicio de Seguridad Comunitario, este apenas sobre pasa el 4.

```{r nota-programas,fig.cap="\\label{fig:nota_programas}Nota de cada programa"}
#remove("nota.infra")

nota.infra <- renca %>%
  dplyr::select(starts_with("q0019_"), -ends_with("_0001")) 

nota.infra <- nota.infra %>% 
  pivot_longer(cols = names(nota.infra))

nota.infra <-  nota.infra %>% 
  filter(!value %in% c(8,9,10)) %>% 
  group_by(name) %>% 
  summarise(mean = mean(value))

nota.infra <- nota.infra %>% 
  mutate(name = factor(name,
                       levels = c("q0019_0001_0002", "q0019_0002_0002", "q0019_0003_0002",
                                  "q0019_0004_0002", "q0019_0005_0002", "q0019_0006_0002",
                                  "q0019_0007_0002", "q0019_0008_0002", "q0019_0009_0002",
                                  "q0019_0010_0002", "q0019_0011_0002", "q0019_0012_0002",
                                  "q0019_0013_0002", "q0019_0014_0002"),
                       labels = c("Farmacia Comunitaria ", "Óptica Comunitaria", 
                       "Servicios de acompañamiento (PADI)", "Renca Jazz", 
                       "Santiago a Mil en Renca", "Programa Fuerza Joven", "Programa 75+", 
                       "Agenda Fácil", "Servicio de seguridad comunitaria 1453",  
                       "Estrategia de prevención de \nviolencia contra la mujer", 
                       "Atención municipal vespertina", "Renca te abriga", 
                       "Escuela del Trabajo" ,"Corrida Familiar")))

ggplot(nota.infra, aes(x = name, y = mean)) +
  geom_point(size = 3) + 
  geom_segment(aes(x = name, xend = name, y = 0, yend = mean)) + 
  scale_y_continuous(limits = c(0, 7)) +
  labs(title = "¿Cómo evaluaría la calidad en una escala de 1 a 7?",
       subtitle = "Evaluación promedio de la muestra",
       x = "", y = "",caption = paste("Promedio = ",round(mean(nota.infra$mean),1))) +
  coord_flip()
```

\newpage

## Conocimiento autoridades

De acuerdo con el Gráfico \ref{fig:conocimiento_alc} existe un alto nivel de conocimiento de la figura del alcalde: un 43% logra identificarlo como "Claudio Castro"; y cerca de un 20% lo conoce o por su nombre o por su apellido.

```{r conoc-alc,fig.cap="\\label{fig:conocimiento_alc}Grado de conocimiento del alcalde"}

alcalde <- renca %>% 
  dplyr::select(q0020) %>% 
  mutate(q0020 = recode(q0020, "NS" = "No sabe", "NR" = "No responde")) %>% 
  count(q0020) %>% 
  mutate(freq = n/sum(n))

alcalde %>%
  ggplot(aes(x = q0020, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Podría decirme el nombre del Alcalde de Renca? ", x = "", y = "") +
  coord_flip()
```

\newpage

Por su parte, en lo que respecta a los concejales, de acuerdo con el Gráfico \ref{fig:conoce_csj}, la mayoría no logra identificar a un concejal (66%, considerando a los No sabe y No responde). El concejal con el mayor nivel de conocimiento sería César Amestica (14%).

```{r conoce-consej,fig.cap="\\label{fig:conoce_csj}Nivel de conocimiento de concejales"}

consejal <- renca %>% 
  dplyr::select(q0021) %>% 
  mutate(q0021 = recode(q0021, "Améstica" = "César Amestica", "César" = "César Amestica",
                        "Avilés" = "Camila Avilés", 
                        "Cecilia" = "Cecilia López", "López" = "Cecilia López",
                        "Diego" = "Diego Fuentes", "Fuentes" = "Diego Fuentes",
                        "Iskra" = "Iskra Calderón",
                        "Sepúlveda" = "Claudio Sepúlveda",
                        "Araya" = "Ismael Araya",
                        "NS" = "No sabe", "NR" = "No responde")) %>% 
  count(q0021) %>% 
  mutate(freq = n/sum(n))

consejal %>%
  ggplot(aes(x = q0021, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Podría decirme el nombre de algún consejal? ", x = "", y = "") + 
  coord_flip()
```

\newpage

# Evaluación gestión municipal

## Evaluación de la Municipalidad de Renca

El Gráfico \ref{fig:gestion} muestra el porcentaje de personas que evalúa de manera al trabajo del municipio según distintas dimensiones. En general, lo que se observa es que entre un tercio y un quinto de los encuestados está de acuerdo con que el municipio ha realizado una gestión positiva en la comuna.

El aspecto que más destaca es el trato que los funcionarios dan a los vecinos: un 32% de los encuestados manifiesta que el trato que reciben es bueno. También destacan la percepción de continua mejora por parte del municipio y de trabajo en terreno, ambos bordeando el 30%.

Los aspectos peor evaluados vendrían siendo el transporte: un 23% de los encuestados considera que el trabajo del municipio en este aspecto es bueno, y participación ciudadana, con un 24%.

```{r eval-gest,fig.cap="\\label{fig:gestion}Porcentaje de personas que evalua positivamente al municipio según áreas de gestión"}
#remove("eval.gest")

eval.gest <- renca %>%
  dplyr::select(starts_with("q0022_")) 

eval.gest <- eval.gest %>% 
  pivot_longer(cols = names(eval.gest)) 

eval.gest <-  eval.gest %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value %in% c(4,5)) %>% 
  summarise(sfreq = sum(freq))

eval.gest <- eval.gest %>% 
  mutate(name = factor(name, 
                       levels = c("q0022_0002", "q0022_0003", "q0022_0004", "q0022_0005", 
                              "q0022_0006","q0022_0007" ,"q0022_0008", "q0022_0009"),
                       labels = c("Da respuesta a los\nproblemas de  las  y los vecinos",
                                  "Es Transparente", "Trabaja en terreno", 
                                  "Permite a la ciudadanía\ncolaborar en la gestión local",
                                  "Apunta al desarrollo\nsustentable de la comuna",
                                  "Promueve el orgullo de ser renquino/a.",
                                  "Funcionarios dan un buen\ntrato a las y los vecinos",
                                  "Está mejorando  su  gestión")))

eval.gest %>%
  ggplot(aes(x = name, y = sfreq, 
             label = as.character(scales::percent(sfreq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Grado de acuerdo con afirmaciones sobre el municipio:", subtitle = "Porcentaje que responde 'de acuerdo o muy de acuerdo'", 
       x = "", y = "") +
  coord_flip()
```

\newpage

## Evaluación elementos del barrio

El Gráfico \ref{fig:eval_barrio} muestra las notas que los encuestados ponen a distintos aspectos del barrio. El promedio del total es de 3.6, y los elementos mejor evaluados son las luminarias públicas y el arbolado, ambos bordeando el 4.0. Por su parte, el elemento peor evaluado son las ciclovías, las que son evaluadas con un 3.3 en promedio, mientras que las señaléticas y los cruces peatonales son evaluados con un 3.5 aproximadamente.

```{r nota-barrio-1,fig.cap="\\label{fig:eval_barrio}Nota a distintas dimensiones de la calidad de vida en el barrio"}
#remove("eval.barrio1")

eval.barrio1 <- renca %>%
  dplyr::select(starts_with("q0023_")) 

eval.barrio1 <- eval.barrio1 %>% 
  pivot_longer(cols = names(eval.barrio1)) 

eval.barrio1 <-  eval.barrio1 %>% 
  filter(!value %in% c(8,9,10)) %>% 
  group_by(name) %>% 
  summarise(mean = mean(value))

eval.barrio1 <- eval.barrio1 %>% 
  mutate(name = factor(name, 
                       levels = c("q0023_0001", "q0023_0002", "q0023_0003", "q0023_0004",
                                  "q0023_0005", "q0023_0006", "q0023_0007" ,"q0023_0008", 
                                  "q0023_0009", "q0023_0010", "q0023_0011"),
                       labels = c("Paraderos", "Calles y avenidas", "Veredas", "Basureros", "Bancas", "Arbolado", "Juego infantiles y calistenias", "Luminarias/alumbrado público", "Cruces peatonales y demarcaciones", "Señalética", "Ciclovías y Ciclosendas")))

ggplot(eval.barrio1, aes(x = name, y = mean)) +
  geom_point(size = 3) + 
  geom_segment(aes(x = name, xend = name, y = 0, yend = mean)) + 
  scale_y_continuous(limits = c(0, 7)) +
  labs(title = "En una escala de 1 a 7 ¿Cómo evaluaría la calidad de?",
       subtitle = "Evaluación promedio de la muestra",
       caption = paste("Promedio = ",round(mean(eval.barrio1$mean),1)),
       x = "", y = "") +
  coord_flip()
```

\newpage

## Evaluación infrestructura y equipamiento barrial

El Gráfico \ref{fig:nota_barrio} muestra la evaluación promedio a distintas infraestructuras barriales. El promedio es de 3.8, y destacan el consultorio donde los encuestados se atienden, que recibe una nota de 4.5, un poco por sobre los jardines, escuelas y liceos municipales. El resto de las infraestructuras recibe una nota bajo el 4, siendo los bandejones y las platabandas los que reciben la evaluación más baja (3.3).

```{r nota-barrio2,fig.cap="\\label{fig:nota_barrio}Nota a infraestructura y equipamiento barrial"}
#remove("eval.barrio2")

eval.barrio2 <- renca %>%
  dplyr::select(starts_with("q0024_")) 

eval.barrio2 <- eval.barrio2 %>% 
  pivot_longer(cols = names(eval.barrio2)) 

eval.barrio2 <-  eval.barrio2 %>% 
  filter(!value %in% c(8,9,10)) %>% 
  group_by(name) %>% 
  summarise(mean = mean(value))

eval.barrio2 <- eval.barrio2 %>% 
  mutate(name = factor(name, 
                       levels = c("q0024_0001", "q0024_0002", "q0024_0003", "q0024_0004",
                                  "q0024_0005", "q0024_0006", "q0024_0007"),
                       labels = c("Plazas de su barrio", "Bandejones y platabandas", 
                                  "Parques  y  áreas  verdes", "Multicanchas de su barrio",
                                  "Sedes sociales", "El consultorio donde ud. se atiende",
                                  "Jardines, Escuelas y Liceos Municipales")))

ggplot(eval.barrio2, aes(x = name, y = mean)) +
  geom_point(size = 3) + 
  geom_segment(aes(x = name, xend = name, y = 0, yend = mean)) + 
  scale_y_continuous(limits = c(0, 7)) +
  labs(title = "En una escala de 1 a 7 ¿Cómo evaluaría la calidad de?",
       subtitle = "Evaluación promedio de la muestra",
       x = "", y = "",
       caption = paste("Promedio = ",round(mean(eval.barrio2$mean),1))) +
  coord_flip()
```

\newpage

## Evaluación servicios municipales

El Gráfico \ref{fig:nota_servmun} muestra las notas que los encuestados ponen a cada uno de los servicios municipales, los que en promedio reciben una nota de 4.1. Destacan los servicios de retiro de basura, con una nota cercana al 5, seguidos de la atención en los centros de salud y la educación recibida en los jardines, escuelas y liceos municipales.

Por otro lado, los servicios que reciben la peor evaluación son los relacionados con seguridad, la atención a emprendedores y el apoyo a las organizaciones comunitarias y de juntas de vecinos. Estas res reciben una nota de 3.7.

```{r eval-servicios,fig.cap="\\label{fig:nota_servmun}Nota a los servicios de la municipalidad"}
#remove("eval.servicios")

eval.servicios <- renca %>%
  dplyr::select(starts_with("q0025_")) 

eval.servicios <- eval.servicios %>% 
  pivot_longer(cols = names(eval.servicios)) 

eval.servicios <-  eval.servicios %>% 
  filter(!value %in% c(8,9,10)) %>% 
  group_by(name) %>% 
  summarise(mean = mean(value))

eval.servicios <- eval.servicios %>% 
  mutate(name = factor(name, 
                       levels = c("q0025_0001", "q0025_0002", "q0025_0003", "q0025_0008",
                                  "q0025_0009", "q0025_0010", "q0025_0011", "q0025_0012",
                                  "q0025_0013", "q0025_0014", "q0025_0015", "q0025_0016",
                                  "q0025_0017", "q0025_0018", "q0025_0019", "q0025_0020"),
                       labels = c("Aseo de la comuna, limpieza de calles y veredas",
                                  "El retiro de basura domiciliaria", 
                                  "La labor preventiva del Municipio \nen Seguridad", 
                                  "La respuesta a emergencias",
                                  "Las actividades de promoción de la cultura \ny las artes", 
                                  "Las  actividades  de  promoción  del  deporte \ny la vida saludable", 
                                  "La entrega de permisos de circulación", 
                                  "El  apoyo  a  la  búsqueda  de  empleo  \ny capacitación laboral", 
                                  "La  calidad  de  la  educación  \nen  los  jardines, escuelas y liceos municipales", 
                                  "La atención en Centros de Salud", "La entrega de licencias de conducir", 
                                  "Ayuda a personas de escasos recursos", 
                                  "Atención al emprendedor(a) \n(tramites, patentes, asesoría, etc.)", 
                                  "La facilitación de buses \npara actividades comunitarias", 
                                  "El apoyo a las Juntas de Vecinos \ny Organizaciones Comunitarias", 
                                  "Los servicios para mascotas entregados \npor la Veterinaria Municipal")))

ggplot(eval.servicios, aes(x = name, y = mean)) +
  geom_point(size = 3) + 
  geom_segment(aes(x = name, xend = name, y = 0, yend = mean)) + 
  scale_y_continuous(limits = c(0, 7)) +
  labs(title = "En una escala de 1 a 7\n¿Cómo evaluaría la calidad de?",
       subtitle = "Evaluación promedio de la muestra",
       x = "", y = "",
       caption = paste("Promedio =",round(mean(eval.servicios$mean),1))) +
  coord_flip()
```

\newpage

# Proyección futuro

## Fortalezas

El Gráfico \ref{fig:fortalezas} muestra las principales fortalezas de la comuna según los encuestados, filtrando aquellas que no alcanzan un 1% de las menciones. En términos generales, se observa que las personas tienden a no identificar una fortaleza en la comuna: cerca del 60% de los encuestados no menciona una fortaleza, ya sea porque no sabe o no responde a la pregunta (56%) o bien porque efectivamente considera que no hay ninguna fortaleza (3%).

Por otro lado, cerca del 10% de los encuestados considera que una de las principales fortalezas de la comuna es el transporte público (lo que en cierta medida se condice con su renuencia a valorar la destrucción del mismo como medio de protesta). Más abajo se encontrarían las ferias y los vecinos como una de las princiaples fortalezas de la comuna, con cerca de un 4% de menciones.

```{r fortalezas, fig.cap="\\label{fig:fortalezas}Principales fortalezas de la comuna"}
#remove("fortalezas")

fortalezas <- renca %>% select(starts_with("q0026"))
fortalezas <- fortalezas %>% pivot_longer(cols = names(fortalezas)) %>% filter(value!="#¡VALOR!") %>%
  mutate(value=ifelse(value %in% c("NS","NR"),"NS/NR",value)) %>% group_by(value) %>% count(value) %>% ungroup() %>%
  mutate(freq=n/sum(n)) %>% filter(freq*100 >= 1)

ggplot(fortalezas,aes(x = value, y = freq, label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Principales fortalezas de la comuna",subtitle="Sólo las menciones con más de un 1%", x = "", y = "") +
  coord_flip()
```

\newpage

## Renca en tres años

El Gráfico \ref{fig:renca_tres} muestra cómo las personas proyectan que estará la comuna en tres años más. En términos generales, se observa que la mayoría de las personas ven que la comuna puede estar mejor (45%) o igual (39%); sólo el 17% considera que estará peor.

```{r proyec-tres, fig.cap="\\label{fig:renca_tres}Poryección de la comuna en tres años más"}
proyec.tres <- renca %>%
  dplyr::select(q0027) %>% 
  filter(!q0027 %in% c(4,5)) %>% 
  as_label(q0027) %>% 
  count(q0027) %>% 
  mutate(freq = n/sum(n)) %>% 
  drop_na()
  

proyec.tres %>%
  ggplot(aes(x = q0027, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Según su percepción, en 3 años Renca estara:", x = "", y = "") 
```

\newpage

## Principales problemas

Por otro lado, cuando se consulta a las personas cuales son las principales problemáticas de la comuna, de acuerdo con el Gráfico \ref{fig:problemas}se observa que la delincuencia y el alacoholismo y la drogadicción son los principales, con un 27% y un 21% de las menciones, respectivamente. El 12% considera que la principal problemática es el acceso al trabajo. Por otro lado, sólo el 3% considera que el principal problema es la recolección de la basura, lo que se condice con los resultados mostrados anteriormente que mostraban que el servicio de basura es uno de los mejor evaluados de la comuna, situación que se repite con la movilización y el transporte y la educación que ofrece la comuna.

```{r tres-problemas, fig.cap="\\label{fig:problemas}Principales problemas de la comuna"}
#remove("tres.problemas")

tres.problemas <- renca %>%
  dplyr::select(starts_with("q0028_"), -q0028_0012, -q0028_0013, -q0028_0014, -q0028_other) %>% 
  as_label()

tres.problemas <- tres.problemas %>% 
  pivot_longer(cols = names(tres.problemas)) 

tres.problemas <-  tres.problemas %>% 
  count(name, value) %>% 
  drop_na() %>% 
  mutate(freq = (n/sum(n)),
         value = recode(value, "Acceso a servicios privados (cajeros automáticos, bancos, supermercados, servipa" = "Acceso a servicios privados")) 

tres.problemas %>%
  ggplot(aes(x = value, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Principales tres problemas de la comuna", 
       subtitle = "Porcentaje de menciones", 
       x = "", y = "") +
  coord_flip()
```

\newpage

# Seguridad

## Reputación barrial

El Gráfico \ref{fig:reputacion_barrio} muestra la percepción que tienen las personas respecto a cómo otros evalúan su barrio. De acuerdo con los datos, la mayoría tiende a considerar que su barrio es visto de manera negativa: un 14% considera que es visto muy negativamente, y un 42% considera que es visto negativamente. Situación que contrasta con el menos del 10% que considera que su barrio es visto positivamente o muy positivamente.

```{r rep-barrial, fig.cap="\\label{fig:reputacion_barrio}Percepción de cómo las personas externas evaluan el barrio"}
rep.barrio <- renca %>%
  dplyr::select(q0029) %>% 
  as_label(q0029) %>% 
  count(q0029) %>% 
  mutate(freq = n/sum(n)) %>% 
  drop_na()
  
rep.barrio %>%
  ggplot(aes(x = q0029, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Cómo cree que evalúan su barrio las personas que viven fuera de él?", x = "", y = "") 
```

\newpage

Si contrastamos estos datos con los reportados por la ELSOC para la RM, se observa que el 44% considera que su barrio es visto de manera positiva, mientras que el 27% considera que es visto de manera negativa.

```{r rep-barrial.elsoc, fig.cap="\\label{fig:reputacion_barrio_elsoc}Percepción de cómo las personas externas evaluan el barrio (ELSOC)"}
rep.barrio.elsoc <- elsoc.sub %>%
  dplyr::select(t08_w06) %>%
  count(t08_w06) %>% 
  mutate(freq = n/sum(n)) %>% 
  drop_na()
  
rep.barrio.elsoc %>%
  ggplot(aes(x = t08_w06, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Cómo cree que evalúan su barrio las personas que viven fuera de él?", x = "", y = "",
       caption = "Fuente: elaboración propia a partir de la ELSOC Ola 6.") 

```

\newpage

## Seguridad en el barrio

El Gráfico \ref{fig:seguridad} muestra el grado de seguridad que sienten las personas en sus barrios. Nuevamente, al igual que en el caso del Gráfico \ref{fig:reputacion_barrio} se ve que la evaluación es más bien negativa. Cerca de un 55% se considera inseguro en su barrio, en contraste con el 13% que opina lo contrario.

```{r segu-barrial,fig.cap="\\label{fig:seguridad}Percepción de seguridad en el barrio"}
segu.barrio <- renca %>%
  dplyr::select(q0030) %>% 
  as_label(q0030) %>% 
  count(q0030) %>% 
  mutate(freq = n/sum(n))
  
segu.barrio %>%
  ggplot(aes(x = q0030, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Qué tan seguro/a o inseguro/a se siente en el barrio", x = "", y = "") 
```

\newpage

Por su parte, cuando se considera al conjunto de la RM se ve que la mayoría tiende a considerarse seguro en su barrio (46%), mientras que el 30% se considera inseguro en su barrio

```{r segu-barrial.elsoc,fig.cap="\\label{fig:seguridad.elsoc}Percepción de seguridad en el barrio (ELSOC)"}
segu.barrio.elsoc <- elsoc.sub %>%
  dplyr::select(t10_w06) %>% 
  as_label(t10_w06) %>% 
  count(t10_w06) %>% 
  mutate(freq = n/sum(n)) %>% na.omit()
  
segu.barrio.elsoc %>%
  ggplot(aes(x = t10_w06, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Qué tan seguro/a o inseguro/a se siente en el barrio", x = "", y = "",
       caption = "Fuente: elaboración propia a partir de la ELSOC Ola 6.") 
```

\newpage

Por su parte, el \ref{fig:seguridad2} muestra el grado de inseguridad que sienten los encuestados en distintas situaciones. Dentro de estas, la que presenta los mayores niveles de percepción de inseguridad es e estar en la calle durante la noche: cerca del 80% de los entrevistados considera que es una situación insegura, lo que es contrasta fuertemente con el estar en la calle durante el día: en tal caso el 47% considera que son inseguras. El resto de las otras situaciones tienen niveles de inseguridad medios: menos del 50% considera que los paraderos, los parques y las ferias son inseguras.

```{r segu-barrio2,fig.cap="\\label{fig:seguridad2}Percepción de riesgo en la comuna según lugar"}
#remove("segu-segu.barrio2")

segu.barrio2 <- renca %>%
  dplyr::select(starts_with("q0031_")) 

segu.barrio2 <- segu.barrio2 %>% 
  pivot_longer(cols = names(segu.barrio2)) 

segu.barrio2 <-  segu.barrio2 %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value %in% c(1,2)) %>% 
  summarise(sfreq = sum(freq))

segu.barrio2 <- segu.barrio2 %>% 
  mutate(name = factor(name, 
                       levels = c("q0031_0001", "q0031_0002", "q0031_0003", 
                                  "q0031_0004", "q0031_0005"),
                       labels = c("En las calles durante el dia",
                                  "En las calles durante la noche",
                                  "En ferias, mercados o almacenes",
                                  "En el transporte público y paraderos",
                                  "En parques u otros espacios públicos")))

segu.barrio2 %>%
  ggplot(aes(x = name, y = sfreq, 
             label = as.character(scales::percent(sfreq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Grado de seguridad en diferentes contextos", subtitle = "Porcentaje que responde 'inseguro o muy inseguro'", 
       x = "", y = "") +
  coord_flip()
```

\newpage

Congruentemente con lo que se ha dicho anteriormente, el Gráfico \ref{fig:miedo.barrio} muestra cuales son las principales fuentes de miedo en los encuestados. En él se ve que, en prácticamente todas las categorías, existe una alta percepción a sufrir un delito, o bien que una persona cercana lo sufra: en todas las categorías, el miedo a sufrir un delito de los enumerados es superior al 70%, y en algunos casos superior al 75%; sólo el miedo a sufrir una violación es ligeramente menor al 70%, alcanzando un 66%.

```{r miedo.barrio,fig.cap="\\label{fig:miedo.barrio}Fuentes de miedo"}
#remove("miedo.barrio")

miedo.barrio <- renca %>%
  dplyr::select(starts_with("q0032_")) 

miedo.barrio <- miedo.barrio %>% 
  pivot_longer(cols = names(miedo.barrio)) 

miedo.barrio <-  miedo.barrio %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value %in% c(4,5)) %>% 
  summarise(sfreq = sum(freq))

miedo.barrio <- miedo.barrio %>% 
  mutate(name = factor(name, 
                       levels = c("q0032_0001", "q0032_0002", "q0032_0003", 
                                  "q0032_0004", "q0032_0005", "q0032_0006"),
                       labels = c("Alguien entre a mi casa", 
                                  "Salir de noche porque podría ser\nvíctima de un delito en la calle",
                                  "Alguien de mi familia sean agredido/a o asaltado/a",
                                  "Ser atacado/a por alguien con un arma",
                                  "Ser acosado o amenazado",
                                  "Ser violado/a o agredido/a sexualmente")))

miedo.barrio %>%
  ggplot(aes(x = name, y = sfreq, 
             label = as.character(scales::percent(sfreq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Grado de acuerdo con sentir miedo de:", 
       subtitle = "Porcentaje que responde 'de acuerdo o muy de acuerdo'", 
       x = "", y = "") +
  coord_flip()
```

\newpage

El Gráfico \ref{fig:crimen.barrio} reporta el porcentaje de personas que ha sido testigo de distintos delitos. Dentro de estos, poco más del 60% declara haber visto consumo de alcohol en la vía pública, venta de droga en la vía pública y rayados en la vía pública (siendo éste el más común). Es de notar también que el 50% de la muestra también declara ser testigo de balaceras o de disparos.

Por otro lado, si se consideran los delitos con menor frecuencia de ser vistos, se logra ver que la violencia intrafamiliar tiene un 21% de testigos. Un porcentaje ligeramente mayor es el que se observa para el caso de los robos en viviendas: en este caso el 26% declara haber sido testigo de uno, porcentaje que contrasta con el número de personas que declara haber visto robos de autos o de vehículos motorizados (35%).

```{r crimen-barrio,fig.cap="\\label{fig:crimen.barrio}Frecuencia con la que se ha visto delitos"}
#remove("crimen.barrio")

crimen.barrio <- renca %>%
  dplyr::select(starts_with("q0033_")) 

crimen.barrio <- crimen.barrio %>% 
  pivot_longer(cols = names(crimen.barrio)) 

crimen.barrio <-  crimen.barrio %>% 
  count(name, value) %>% 
  group_by(name) %>% 
  mutate(freq = (n/sum(n))) %>% 
  filter(value %in% c(4,5)) %>% 
  summarise(sfreq = sum(freq))

crimen.barrio <- crimen.barrio %>% 
  mutate(name = factor(name, 
                       levels = c("q0033_0001", "q0033_0002", "q0033_0003", "q0033_0004", 
                                  "q0033_0005", "q0033_0006", "q0033_0007", 
                                  "q0033_0009", "q0033_0010", "q0033_0011", "q0033_0012",
                                  "q0033_0013", "q0033_0015", "q0033_0016"),
                       labels = c('Consumo de alcohol y/o droga en la  vía  publica,  parques  o  plazas',
                                  'Peleas callejeras con armas blancas o de fuego',
                                  'Robo  o  asaltos  en  la  vía  pública', 
                                  'Venta de droga y/u otras sustancias  ilícitas',
                                  'Escucha balaceras o disparos',
                                  'Amenazas  o  peleas  entre  vecinos/as',
                                  'Ruidos molestos',
                                  'Problemas o molestia por presencia de perros vagos',
                                  'Rayados o marcas en la propiedad  pública  o  privada\n(grafitis, pinturas  o  murales  sin  autorización)',
                                  'Sitios   eriazos   o   descuidados/acumulación  de  basura',
                                  'Robo en viviendas particulares',
                                  'Robo  o  hurto  de  vehículos  motorizados',
                                  'Violencia intrafamiliar',
                                  'Niños/as  solos  en  las  calles')))

crimen.barrio %>%
  ggplot(aes(x = name, y = sfreq, 
             label = as.character(scales::percent(sfreq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.3, hjust = -0.3) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Con qué frecuencia ha visto", 
       subtitle = "Porcentaje que responde 'a menudo o siempre'", 
       x = "", y = "") +
  coord_flip()
```

\newpage

# Caracterización sociodemográfica

```{r sexo}
sexo <- renca %>%
  dplyr::select(q0034) %>% 
  as_label(q0034) %>% 
  count(q0034) %>% 
  mutate(freq = n/sum(n))
  
sexo %>%
  ggplot(aes(x = q0034, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Sexo de los/as encuestados/as", x = "", y = "") 
```

```{r edad}
edad.cat <- renca %>%
  dplyr::select(q0035) %>% 
  mutate(edad = cut(as.numeric(renca$q0035),breaks=c(17,25,35,45,55,65,85),
                    labels=c("18-25","26-35","36-45","46-55","56-65", "66 o más"))) %>% 
  count(edad) %>% 
  mutate(freq = n/sum(n))

edad.cat %>%
  ggplot(aes(x = edad, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Edad de los/as encuestados/as", x = "", y = "") 
```

```{r nacionalidad}
nacion <- renca %>%
  dplyr::select(q0036) %>% 
  as_label(q0036) %>% 
  count(q0036) %>% 
  mutate(freq = n/sum(n))

nacion %>%
  ggplot(aes(x = q0036, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Nacionalidad de los/as encuestados/as", x = "", y = "") 
```

```{r pueblo-orig}
pueb.orig <- renca %>%
  dplyr::select(q0037) %>% 
  as_label(q0037) %>% 
  count(q0037) %>% 
  mutate(freq = n/sum(n))

pueb.orig %>%
  ggplot(aes(x = q0037, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Se considera perteneciente a algún pueblo indígena u originario?", x = "", y = "") 
```

```{r anos-comuna}

annos.com <- renca %>%
  dplyr::select(q0038) %>% 
  mutate(annos = cut(as.numeric(renca$q0038),breaks = c(0,10,20,30,40,50,80),
                            labels=c("1-10","11-20","21-30","31-40","41-50","61 o más"))) %>% 
  count(annos) %>% 
  mutate(freq = n/sum(n))

annos.com %>%
  ggplot(aes(x = annos, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = -0.8, hjust = 0.5) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Desde hace cuántos años vive en esta comuna?", x = "", y = "") 
```

```{r educacion}
nvl.educ <- renca %>%
  dplyr::select(q0039) %>% 
  filter(!q0039 %in% c(9,10)) %>% 
  as_label(q0039) %>% 
  count(q0039) %>% 
  mutate(freq = n/sum(n))

nvl.educ %>%
  ggplot(aes(x = q0039, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Se considera perteneciente a algún pueblo indígena u originario?", x = "", y = "") +
  coord_flip()
```

```{r trabajo}
#remove("trabajo")
trabajo <- renca %>%
  dplyr::select(q0040) %>% 
  as_label(q0040) %>% 
  count(q0040, sort = TRUE) %>% 
  mutate(freq = n/sum(n))

trabajo %>%
  ggplot(aes(x = q0040, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Tomando en cuenta la semana pasada ¿Usted...?", x = "", y = "") +
  coord_flip()
```

```{r sit-laboral}
sit.trabajo <- renca %>%
  dplyr::select(q0041) %>% 
  filter(!q0041 %in% c(7,8)) %>% 
  as_label(q0041) %>% 
  count(q0041, sort = TRUE) %>% 
  mutate(freq = n/sum(n),
         q0041 = recode(q0041, "Tenía empleo, pero estaba de vacaciones, con licencia, en descanso, etc." = "Tenía empleo, pero estaba de vacaciones,\ncon licencia, en descanso, etc.")) %>% 
  drop_na

sit.trabajo %>%
  ggplot(aes(x = q0041, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Si no trabajó, ¿Se encontraba en alguna de\nlas siguientes situaciones?:", x = "", y = "") +
  coord_flip()
```

```{r ocupacion}
ocupacion <- renca %>%
  dplyr::select(q0044) %>% 
  filter(!q0044 %in% c(8,9)) %>% 
  as_label(q0044) %>% 
  count(q0044, sort = TRUE) %>% 
  mutate(freq = n/sum(n)) %>% 
  drop_na()

ocupacion %>%
  ggplot(aes(x = q0044, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Si trabaja, en este trabajo usted es...", x = "", y = "") +
  coord_flip()
```

```{r sost-hogar}
sost.hogar <- renca %>%
  dplyr::select(q0044) %>% 
  as_label(q0044) %>% 
  count(q0044, sort = TRUE) %>% 
  mutate(freq = n/sum(n)) %>% 
  drop_na()

sost.hogar %>%
  ggplot(aes(x = q0044, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Es usted el principal sostenedor del hogar?", x = "", y = "") +
  coord_flip()
```

```{r educ-jefe}
educ.sost <- renca %>%
  dplyr::select(q0045) %>% 
  filter(!q0045 %in% c(9,10)) %>% 
  as_label(q0045) %>% 
  count(q0045, sort = TRUE) %>% 
  mutate(freq = n/sum(n)) %>% 
  drop_na()

educ.sost %>%
  ggplot(aes(x = q0045, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "¿Cuál es el nivel educacional del principal sostenedor\ndel hogar?", x = "", y = "") +
  coord_flip()
```

```{r trabajo-jefe}
trab.sost <- renca %>%
  dplyr::select(q0046) %>% 
  filter(!q0046 %in% c(10,11)) %>% 
  as_label(q0046) %>% 
  count(q0046, sort = TRUE) %>% 
  mutate(freq = n/sum(n)) %>% 
  drop_na()

trab.sost %>%
  ggplot(aes(x = q0046, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Actividad principal del sostenedor\ndel hogar durante el último mes", x = "", y = "") +
  coord_flip()
```

```{r ingresos}
## Ingresos totales
ingresos <- renca %>%
  dplyr::select(q0048) %>% 
  filter(!q0048 %in% c(8,9)) %>% 
  mutate(ingre_hog  = factor(q0048, labels = c("Menos de $250.000","$250.000 - $350.000",
                                               "$350.000 - $450.000","$450.000 - $700.000",
                                               "$700.000 - $800.000","$900.000 - $1.00.0000",
                                               "Más de $1.000.000"))) %>% 
  count(ingre_hog) %>% 
  mutate(freq = n/sum(n)) %>% 
  drop_na()

ingresos %>%
  ggplot(aes(x = ingre_hog, y = freq, 
             label = as.character(scales::percent(freq, accuracy = .1)))) +
  geom_col(position = 'dodge2') + 
  geom_text(size = 5, vjust = 0.5, hjust = -0.1) +
  scale_y_continuous(labels = scales::percent, limits = c(0, 1)) +
  labs(title = "Edad de los/as encuestados/as", x = "", y = "") +
  coord_flip()

```
